#include <bits/stdc++.h> // el include da byinclude kol el stls y3ny el queue w stack w map w set w kol 7aga 7rfyn + iostream w 7bt 7gat kman kda
// da el standard fel competetive problem solving bs kda.goodgood
// kman fe automatic formater by4t8l lma t3ml ctrl + s
using namespace std;

#define int long long // el line da by5ly kol mra tktb klmt int y2raha k2nha long long
#define FIO                           \
    ios_base::sync_with_stdio(false); \
    cin.tie(nullptr);                 \
    cin.tie(nullptr); // dol by5lo cpp ta5od input asr3 howa 3mtn mbyfr24 y3ny bs hya 7rka rw4a y3ny f why not

void sol()
{ // dos ctrl+s byzbt el indentations w kol 7aga
    // msh kda lw 7d shafo hay7so chatgptnonononnoo
    // el by7ss eno chat gpt howa asamy el variables w el comments
    // bs en enta tkon kateb clean code 34an t3rf t2rah da m4 3eb wla ay 7aga
    // w kol el nas btst5dmn
    // kman 4ayf el 7gate le 7mra el bttl3 de
          // de esmha trailing white spaces el hya en yb2a fe spaces zyada w mfe4 b3deha 7aga f btb2a w5da space 3l fady
          // t2dr t4elha lw 3mlt backslash + ]

    // da lzez da bos
    
    for (int i = 0; i < 10; i++)
    {
        cout << "hi" << endl;
    }
    // bs kda
    // lw el problem bta3k fe test cases e3ml uncomment l da
    // w el solution bta3k ektbo hna
    // w howa kol test case hy call el function de
    return;
} // lw el size bta3 el variables kber m4 hyfr2 m3ak fel time complextiy bs lw el size bta3 le variables asln so8yr m4 mstahl long long w enta est5dmt long long hyfr3 m3af fl SPACE complexity bs el TIME l2 bs kda kda el space mbyfr24 fel competitive programming w asln el fr2 byb2a 7rfyn bdl 4 byte for int byb2a 8 byte for long long
int32_t main() // el int hna 4klha 8reb 34an el main function f cpp lazem t return int w e7na 3mlna override l klmt int b2a el compiler by2raha long long f hydy error f bnst5dm da
// int32_t da hya hya int bs 34an e7na dlwa2ty lw ktbna int hyboze look
{
    // git lens da emfrood eno aktr extention most5dm 3la vs code asln bs ana lsa mgrbto4 bs emfrood eno gamed f45 
    // howa extention github y3ny
    // howa 7rfyn 48al autonmatic
    // 4klo lzez 3mtn
    FIO; // el line da by initialize dol
    int t = 1;
    // cin >> t;
    while (t--)
    {
        sol();
    }
    return 0;
}